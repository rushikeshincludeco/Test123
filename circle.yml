machine:
  xcode:
    version: "8.3.2"
  environment:
    GYM_CODE_SIGNING_IDENTITY: "iPhone Distribution: Include Technologies (8DMCVMABTV)"
deployment:
  hockey:
    branch: master
    commands:
       - xcodebuild archive -workspace <T1>.xcworkspace -scheme <T1> -archivePath </Library/Developer/Xcode/Archives>.xcarchive